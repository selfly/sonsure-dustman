server:
  port: 8001
  tomcat:
    uri-encoding: utf-8
spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:~/dustman;mode=mysql;database_to_lower=true
    driver-class-name: org.h2.Driver
    username: sa